<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="my.edu.tarc.myhello.MainActivity">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Hello World!"
        android:id="@+id/TextViewMessage" />

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:srcCompat="@drawable/apple_pen"
        android:layout_below="@+id/TextViewMessage"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="59dp"
        android:id="@+id/imageViewApplePen" />

    <Button
        android:text="@string/hide_image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="33dp"
        android:id="@+id/buttonHideImage"
        android:layout_below="@+id/buttonShowMessage"
        android:layout_alignLeft="@+id/buttonShowMessage"
        android:layout_alignStart="@+id/buttonShowMessage"
        android:onClick="hideImage" />

    <Button
        android:text="@string/click_me"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/buttonShowMessage"
        android:onClick="showMessage"
        android:layout_centerVertical="true"
        android:layout_alignRight="@+id/imageViewApplePen"
        android:layout_alignEnd="@+id/imageViewApplePen" />
</RelativeLayout>
